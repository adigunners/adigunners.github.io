// Test JavaScript file for auto-formatting verification
function testFunction(param1, param2) {
  const result = param1 + param2;
  if (result > 10) {
    return 'large';
  } else {
    return 'small';
  }
}

const array = [1, 2, 3, 4, 5];
const object = { name: 'test', value: 123, nested: { key: 'value' } };

console.log(testFunction(5, 7));
